{
  "name": "pavo-tweak-suite",
  "version": "1.0.0",
  "description": "Pavo Tweak Suite - Unified Backend and Discord Bot",
  "main": "start-unified.js",
  "scripts": {
    "install-all": "cd backend && npm install && cd ../pavo-bot && npm install",
    "postinstall": "cd backend && npm install && cd ../pavo-bot && npm install",
    "start": "node start-unified.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "discord.js": "^14.15.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.3.1",
    "helmet": "^7.1.0"
  },
  "engines": {
    "node": "22.x"
  }
}
